import 'mocha';
import { expect } from 'chai';
import 'reflect-metadata';

describe('Command Decorator', () => {
    beforeEach(() => {});

    it('Should allow a class to be instantiated', () => {});
});
